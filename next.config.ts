import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Uncomment on Day 4, Section 4 to see the bundle each route ships.
  // experimental: { webpackBuildWorker: true },
};

export default nextConfig;
