# Day 4 — leave API_BASE_URL unset. The app then calls its own Next.js route
# handlers in src/app/api/v1, which serve the agreed JakOne contract from
# static JSON. No database, no Docker, no backend required.

# Day 7 — point the same code at the real Spring Boot service:
# API_BASE_URL=http://localhost:8080
